import { existsSync, readFileSync } from 'node:fs';
import assert from 'node:assert/strict';
import { dirname, resolve } from 'node:path';

function readCssWithImports(filePath, seen = new Set()) {
  const fullPath = resolve(filePath);
  if (seen.has(fullPath)) return '';
  seen.add(fullPath);
  const source = readFileSync(fullPath, 'utf8');
  const imports = [...source.matchAll(/@import\s+['"](.+?)['"];/g)]
    .map((match) => readCssWithImports(resolve(dirname(fullPath), match[1]), seen));
  return [source, ...imports].join('\n');
}

const settingsPage = readFileSync('src/pages/SettingsPage.tsx', 'utf8');
const settingsPageOptions = readFileSync('src/pages/useSettingsPageOptions.ts', 'utf8');
const settingsPagePropsBuilders = readFileSync('src/pages/SettingsPagePropsBuilders.ts', 'utf8');
const settingsCenterChapterDiagnosticsModel = readFileSync('src/features/settings-center/settingsCenterChapterDiagnosticsModel.ts', 'utf8');
const settingsCenterChapterActions = readFileSync('src/features/settings-center/settingsCenterChapterActions.ts', 'utf8');
const settingsCenterImportModelSource = readFileSync('src/features/settings-center/settingsCenterImportModel.ts', 'utf8');
const settingsCenterImportExport = readFileSync('src/services/settingsCenter/importExport.ts', 'utf8');
const settingsCenterImportModel = `${settingsCenterImportModelSource}\n${settingsCenterImportExport}`;
const settingsCenterModel = readFileSync('src/features/settings-center/settingsCenterModel.ts', 'utf8');
const settingsCenterPageModel = readFileSync('src/features/settings-center/settingsCenterPageModel.ts', 'utf8');
const settingsCenterPathDisplayModel = readFileSync('src/features/settings-center/settingsCenterPathDisplayModel.ts', 'utf8');
const settingsCenterDataMaintenanceActions = readFileSync('src/features/settings-center/settingsCenterDataMaintenanceActions.ts', 'utf8');
const settingsCenterSecurityActionsPath = 'src/features/settings-center/settingsCenterSecurityActions.ts';
assert.ok(existsSync(settingsCenterSecurityActionsPath), 'settings security actions must live in settingsCenterSecurityActions');
const settingsCenterSecurityActions = readFileSync(settingsCenterSecurityActionsPath, 'utf8');
const settingsCenterDefaultResetActions = readFileSync('src/features/settings-center/settingsCenterDefaultResetActions.ts', 'utf8');
const settingsCenterSnapshotActions = readFileSync('src/features/settings-center/settingsCenterSnapshotActions.ts', 'utf8');
const settingsCenterReaderCleanupModel = readFileSync('src/features/settings-center/settingsCenterReaderCleanupModel.ts', 'utf8');
const settingsCenterStatusFormatModel = readFileSync('src/features/settings-center/settingsCenterStatusFormatModel.ts', 'utf8');
const settingsCenterServiceSource = readFileSync('src/services/settingsCenterService.ts', 'utf8');
const settingsCenterDefaults = readFileSync('src/services/settingsCenter/defaults.ts', 'utf8');
const settingsCenterSchema = readFileSync('src/services/settingsCenter/schema.ts', 'utf8');
const settingsCenterMigrations = readFileSync('src/services/settingsCenter/migrations.ts', 'utf8');
const settingsCenterPersistence = readFileSync('src/services/settingsCenter/persistence.ts', 'utf8');
const settingsCenterFacade = readFileSync('src/services/settingsCenter/service.ts', 'utf8');
const settingsCenterService = `${settingsCenterServiceSource}\n${settingsCenterDefaults}\n${settingsCenterSchema}\n${settingsCenterMigrations}\n${settingsCenterPersistence}\n${settingsCenterImportExport}\n${settingsCenterFacade}`;
const systemDiagnosticsService = readFileSync('src/services/systemDiagnosticsService.ts', 'utf8');
const app = readFileSync('src/app/App.tsx', 'utf8');
const appTaskCenterHook = readFileSync('src/app/useTaskCenter.ts', 'utf8');
const appLibraryHook = readFileSync('src/app/useLibrary.ts', 'utf8');
const appShellModel = readFileSync('src/app/appShellModel.ts', 'utf8');
const appTaskActions = readFileSync('src/app/appTaskActions.ts', 'utf8');
const settingsChapterPanel = readFileSync('src/pages/SettingsChapterPanel.tsx', 'utf8');
const settingsAiPanel = readFileSync('src/pages/SettingsAiPanel.tsx', 'utf8');
const settingsSupplementalPanels = readFileSync('src/pages/SettingsSupplementalPanels.tsx', 'utf8');
const settingsDiagnosticsPanel = readFileSync('src/pages/SettingsDiagnosticsPanel.tsx', 'utf8');
const settingsSearchPanel = readFileSync('src/pages/SettingsSearchPanel.tsx', 'utf8');
const settingsDataPanel = readFileSync('src/pages/SettingsDataPanel.tsx', 'utf8');
const readerPage = readFileSync('src/features/reader-core/ReaderPage.tsx', 'utf8');
const readerPageMeasurementController = readFileSync('src/features/reader-core/useReaderPageMeasurementController.ts', 'utf8');
const readerModel = readFileSync('src/features/reader-core/readerModel.ts', 'utf8');
const readerChapterParserModel = readFileSync('src/features/reader-core/readerChapterParserModel.ts', 'utf8');
const readerSettingsPanel = readFileSync('src/features/reader-core/ReaderSettingsPanel.tsx', 'utf8');
const readerWorkspace = readFileSync('src/pages/ReaderWorkspace.tsx', 'utf8');
const readerWorkspaceDocument = readFileSync('src/pages/reader-workspace/useReaderWorkspaceDocument.ts', 'utf8');
const readerWorkspacePersistence = readFileSync('src/pages/reader-workspace/useReaderWorkspacePersistence.ts', 'utf8');
const readerWorkspaceState = readFileSync('src/pages/reader-workspace/useReaderWorkspaceState.ts', 'utf8');
const readerWorkspaceDataActions = readFileSync('src/pages/readerWorkspaceDataActions.ts', 'utf8');
const readerWorkspaceModel = readFileSync('src/pages/readerWorkspaceModel.ts', 'utf8');
const readerDocumentWorker = readFileSync('src/features/reader-core/readerDocumentWorker.ts', 'utf8');
const aiReaderPanel = readFileSync('src/features/ai-reader/AiReaderPanel.tsx', 'utf8');
const readerStorageService = readFileSync('src/services/readerStorageService.ts', 'utf8');
const taskService = readFileSync('src/services/taskService.ts', 'utf8');
const taskCenterState = readFileSync('src/features/task-center/useTaskCenterState.ts', 'utf8');
const styles = readCssWithImports('src/app/styles.css');
const tauriCommands = `${readFileSync('src-tauri/src/commands.rs', 'utf8')}\n${readFileSync('src-tauri/src/commands/reader_data_commands.rs', 'utf8')}\n${readFileSync('src-tauri/src/commands/task_commands.rs', 'utf8')}`;
const tauriReaderData = readFileSync('src-tauri/src/reader_data.rs', 'utf8');
const tauriDatabase = readFileSync('src-tauri/src/database.rs', 'utf8');
const databaseIndexMaintenanceBody = tauriDatabase.match(/pub\(crate\) fn rebuild_sqlite_database_indexes_in[\s\S]*?\n}\n\npub\(crate\) fn save_chunks_to_fts/)?.[0] ?? '';
const databaseVacuumBody = tauriDatabase.match(/pub\(crate\) fn vacuum_sqlite_database_in[\s\S]*?\n}\n\npub\(crate\) fn save_chunks_to_fts/)?.[0] ?? '';
assert.match(settingsPage, /createSettingsDataMaintenanceActions/, 'settings page must delegate data maintenance actions to the settings-center feature module');
assert.match(settingsCenterDataMaintenanceActions, /loadSettingsDiagnosticPaths/, 'settings center data maintenance actions must load diagnostic filesystem paths');
assert.match(settingsCenterDataMaintenanceActions, /loadDataDirectoryStatus/, 'settings center data maintenance actions must use the settings service effective data directory for path display');
assert.match(settingsCenterPathDisplayModel, /normalizeDisplayPath/, 'settings center path display model must normalize local paths for display');
assert.match(settingsCenterPathDisplayModel, /joinDisplayPath/, 'settings center path display model must join display paths consistently');
assert.match(settingsCenterDataMaintenanceActions, /normalizeDisplayPath\(status\.dataDir\)/, 'settings data maintenance actions must delegate data directory path normalization to the path display model');
assert.match(settingsCenterService, /get_data_directory_status/, 'settings center service must invoke the backend effective data directory command');
assert.match(settingsCenterDataMaintenanceActions, /settings\.json/, 'settings center must show the settings file path');
assert.match(settingsCenterDataMaintenanceActions, /bookmind\.sqlite/, 'settings center must show the FTS database path');
assert.match(settingsCenterService, /export type LocalEncryptionStatus/, 'settings center service must type local encryption status');
assert.match(settingsCenterService, /activeKeyId:\s*string/, 'local encryption status must expose an active key id for rotation diagnostics');
assert.match(settingsCenterService, /retiredKeyCount:\s*number/, 'local encryption status must expose retired key count');
assert.match(settingsCenterService, /masterPasswordEnabled:\s*boolean/, 'local encryption status must expose master password state');
assert.match(settingsCenterService, /fallbackProtection:\s*'masterPassword' \| 'plaintextFile' \| 'keyringOnly' \| 'none' \| string/, 'local encryption status must type fallback protection mode');
assert.match(settingsCenterService, /export type LocalDataKeyRotationResult/, 'settings center service must type local data key rotation results');
assert.match(settingsCenterService, /get_local_encryption_status/, 'settings center service must invoke the backend local encryption status command');
assert.match(settingsPage, /localEncryptionStatus/, 'settings center must keep local encryption status in state');
assert.match(settingsPage, /createSettingsSecurityActions\(/, 'settings page must delegate security actions to the settings-center feature module');
assert.match(settingsCenterSecurityActions, /loadLocalEncryptionStatus/, 'settings security actions must load local encryption status');
assert.match(settingsCenterStatusFormatModel, /formatLocalEncryptionKeyStatus/, 'settings center status format model must format local encryption key status');
assert.match(settingsCenterStatusFormatModel, /formatAiApiKeyStorageStatus/, 'settings center status format model must format AI API key storage status');
assert.match(settingsCenterStatusFormatModel, /formatDataDirectoryMode/, 'settings center status format model must format data directory modes');
assert.match(settingsDataPanel, /settings\.data\.encryptionStatus\.keyStatus/, 'data privacy settings must render localized local encryption key status');
assert.match(settingsDataPanel, /settings\.data\.masterPassword\.title/, 'data privacy settings must render the localized custom master password control');
assert.match(settingsCenterSecurityActions, /setLocalMasterPassword/, 'settings security actions must call the backend command to set the local master password');
assert.match(settingsCenterSecurityActions, /verifyLocalMasterPassword/, 'settings security actions must call the backend command to verify the local master password');
assert.match(settingsDataPanel, /settings\.data\.masterPassword\.boundary/, 'master password UI must state its localized fallback/keyring boundary');
assert.match(settingsDataPanel, /settings\.data\.keyRotation\.title/, 'data privacy settings must render local data key rotation controls');
assert.match(settingsCenterSecurityActions, /rotateLocalDataKey/, 'settings security actions must call the backend command to rotate the local data key');
assert.match(settingsDataPanel, /settings\.data\.keyRotation\.(masterPasswordRequired|noMasterPassword)/, 'key rotation UI must explain its current key protection requirement');
assert.match(settingsDataPanel, /AES-256-GCM/, 'encryption status UI must show the local envelope cipher');
assert.match(settingsDataPanel, /localEnvelopeV1|envelopeVersion/, 'encryption status UI must show the envelope version');
assert.match(settingsCenterModel, /export const encryptionCoverageMatrix/, 'settings center model must define a local encryption coverage matrix');
assert.match(settingsPagePropsBuilders, /encryptionCoverageMatrix/, 'settings page prop builders must render the shared local encryption coverage matrix');
assert.match(settingsDataPanel, /settings\.data\.encryptionCoverage\.title/, 'data privacy settings must render localized encryption coverage');
assert.match(settingsDataPanel, /settings-encryption-coverage/, 'encryption coverage must use a readable matrix class');
assert.match(settingsDataPanel, /localEncryptionStatus\?\.protectedKinds\.includes\(item\.kind\)/, 'encryption coverage must compare expected protected data with backend reported protected kinds');
assert.match(settingsDataPanel, /settings\.data\.encryptionCoverage\.unprotected/, 'encryption coverage must clearly mark unencrypted or not-yet-covered data classes');
assert.match(settingsPageOptions, /getPrivacyFilePath\(localEncryptionStatus\.fallbackFilePath,\s*extendedSettings\)/, 'encryption fallback key path must respect path privacy settings');
assert.match(systemDiagnosticsService, /masterPasswordEnabled:\s*Boolean\(status\.masterPasswordEnabled\)/, 'system diagnostics must include master password status without secrets');
assert.match(systemDiagnosticsService, /activeKeyId:\s*status\.activeKeyId \? hashDiagnosticLabel\(status\.activeKeyId\) : ''/, 'system diagnostics must hash the local active key id');
assert.doesNotMatch(settingsPage, /dataKey|secretKey|rawKey|keyMaterial/, 'settings UI must not render key material fields');
assert.match(settingsPageOptions, /value: 'system'/, 'settings center options hook must expose system theme choices');
assert.match(settingsPageOptions, /value: 'oled'/, 'settings center options hook must expose the reader OLED theme choice');
assert.match(settingsCenterService, /export type AiApiKeyStorageStatus/, 'settings center service must type AI API key storage status');
assert.match(settingsCenterService, /get_ai_api_key_storage_status/, 'settings center service must invoke the backend AI API key storage status command');
assert.match(settingsPage, /aiApiKeyStorageStatus/, 'settings center must keep AI API key storage status in state');
assert.match(settingsCenterSecurityActions, /loadAiApiKeyStorageStatus/, 'settings security actions must load AI API key storage status');
assert.match(settingsAiPanel, /settings\.aiPanel\.diagnostics\.keyStatus/, 'AI settings must render localized API key storage status');
assert.match(settingsAiPanel, /formatAiApiKeyStorageStatus\(/, 'AI API key storage status UI must delegate keyring, fallback-file, and unsaved state formatting to the status model');
assert.match(settingsPageOptions, /getPrivacyFilePath\(aiApiKeyStorageStatus\.fallbackFilePath,\s*extendedSettings\)/, 'AI API key fallback path must respect path privacy settings');
assert.match(settingsCenterDataMaintenanceActions, /clearOperationLogSettingsData/, 'settings center must provide a clear operation logs action');
assert.match(settingsCenterDataMaintenanceActions, /exportOperationLogsSettingsData/, 'settings center must provide an operation logs export action');
assert.match(settingsPage, /operationLogFilterLevel/, 'settings center must keep an operation log level filter state');
assert.match(settingsPage, /operationLogFilterQuery/, 'settings center must keep an operation log keyword filter state');
assert.match(settingsPage, /filteredOperationLogs/, 'settings center must render operation logs through a filtered list');
assert.match(settingsDiagnosticsPanel, /settings\.diagnostics\.logs\.filter\.title/, 'developer diagnostics settings must render localized operation log filtering controls');
assert.match(settingsPageOptions, /settings\.options\.page\.operationLogFilter\.all/, 'operation log filtering must support viewing all levels');
assert.match(settingsCenterDataMaintenanceActions, /bookmind-operation-logs-\$\{date\}\.json/, 'operation logs export must support JSON download');
assert.match(settingsCenterDataMaintenanceActions, /bookmind-operation-logs-\$\{date\}\.ndjson/, 'operation logs export must support NDJSON download');
assert.match(settingsCenterDataMaintenanceActions, /clearReaderPageCacheSettingsData/, 'settings center must provide a clear reader pagination cache action');
assert.match(settingsCenterDataMaintenanceActions, /clearCurrentBookReaderPageCacheSettingsData/, 'settings center must provide a current-book pagination cache clear action');
assert.match(settingsCenterDataMaintenanceActions, /clearAllReaderPageCacheSettingsData/, 'settings center must provide an all-books pagination cache clear action');
assert.match(settingsCenterDataMaintenanceActions, /readerCacheRecordKinds/, 'settings center must define the reader cache record kinds separately from user annotations');
assert.match(settingsCenterDataMaintenanceActions, /readerCacheRecordKinds[^\n]*'state'[^\n]*'pageCache'[^\n]*'readingStats'/, 'reader cache clearing must include saved state, pagination cache, and reading statistics');
assert.doesNotMatch(settingsCenterDataMaintenanceActions, /readerCacheRecordKinds[^\n]*(?:'highlights'|'bookmarks'|'tocEdits')/, 'reader cache clearing must not delete annotations, bookmarks, or TOC edits');
assert.match(settingsCenterDataMaintenanceActions, /clearCurrentBookReaderCacheSettingsData/, 'settings center must provide a current-book full reader cache clear action');
assert.match(settingsCenterDataMaintenanceActions, /clearAllReaderCacheSettingsData/, 'settings center must provide an all-books full reader cache clear action');
assert.match(settingsCenterDataMaintenanceActions, /emitReaderCacheCleared\(\)/, 'settings center must broadcast full reader cache clearing through the typed reader event');
assert.match(readerPageMeasurementController, /subscribeReaderCacheCleared/, 'reader page measurement controller must react to typed full reader cache clear events');
assert.match(settingsCenterDataMaintenanceActions, /currentBookDataRecordKinds/, 'settings center must define all current-book reader data record kinds for destructive per-book clearing');
for (const kind of ['state', 'highlights', 'bookmarks', 'highlightColor', 'tocEdits', 'tocManifest', 'pageCache', 'readingStats', 'chapterTimeline', 'cloudAiRequestHistory', 'cloudAiConsent', 'aiModePreference']) {
  assert.match(settingsCenterDataMaintenanceActions, new RegExp(`currentBookDataRecordKinds[\\s\\S]*'${kind}'`), `current-book all-data clearing must include ${kind}`);
}
assert.match(settingsCenterDataMaintenanceActions, /clearCurrentBookAllDataSettingsData/, 'settings center must provide a current-book all-data clear action');
assert.match(settingsCenterPageModel, /clearCurrentBookAllDataLocalStorage/, 'settings center page model must clear legacy per-book reader localStorage for all current-book data');
assert.match(readerStorageService, /deleteReaderRecordsByBook/, 'reader storage service must expose book-scoped reader record deletion');
assert.match(readerStorageService, /delete_reader_records_by_book/, 'reader storage service must invoke book-scoped backend deletion');
assert.match(settingsCenterDataMaintenanceActions, /deleteReaderRecordsByBook\(currentBookId\)/, 'current-book all-data clearing must delete all reader records for the active book, including quarantine rows');
assert.match(settingsCenterReaderCleanupModel, /bookmind:reader-highlights-index:\$\{bookId\}/, 'current-book all-data clearing must remove legacy highight index localStorage');
assert.match(settingsCenterReaderCleanupModel, /bookmind:reader-highlight-entry:\$\{bookId\}:/, 'current-book all-data clearing must remove legacy split highlight entry localStorage');
assert.match(settingsCenterPageModel, /buildCurrentBookAllDataLocalStoragePlan\(bookId\)/, 'settings center page model must delegate legacy current-book localStorage cleanup key calculation to the reader cleanup model');
assert.match(settingsCenterDataMaintenanceActions, /publishCurrentBookDataCleared\(detail\)/, 'settings center must publish current-book all-data clearing through the typed reader event');
assert.match(readerWorkspacePersistence, /subscribeCurrentBookDataCleared/, 'reader workspace persistence hook must react to typed current-book all-data clear events to avoid rewriting stale in-memory data');
assert.match(readerWorkspacePersistence, /suppressReaderPersistenceForBookDataClear/, 'reader workspace persistence hook must suppress the immediate persistence pass triggered by current-book all-data clearing');
assert.match(readerWorkspacePersistence, /consumeReaderPersistenceSuppression\(book\.id,\s*'state'\)/, 'reader workspace persistence hook must not rewrite reader state after current-book all-data clearing');
assert.match(readerWorkspacePersistence, /consumeReaderPersistenceSuppression\(book\.id,\s*'highlights'\)/, 'reader workspace persistence hook must not rewrite highlights after current-book all-data clearing');
assert.match(readerWorkspacePersistence, /consumeReaderPersistenceSuppression\(book\.id,\s*'bookmarks'\)/, 'reader workspace persistence hook must not rewrite bookmarks after current-book all-data clearing');
assert.match(readerWorkspacePersistence, /consumeReaderPersistenceSuppression\(book\.id,\s*'tocEdits'\)/, 'reader workspace persistence hook must not rewrite TOC edits after current-book all-data clearing');
assert.match(aiReaderPanel, /subscribeCurrentBookDataCleared/, 'AI reader panel must reset per-book consent and mode after typed current-book all-data clearing');
assert.match(aiReaderPanel, /setQuestion\(''\)[\s\S]*setAttachments\(\[\]\)[\s\S]*setSelectedSlashCommand\(null\)/, 'AI reader panel must clear local prompt, attachment, and slash-command draft state after current-book all-data clearing');
assert.match(app, /resetAiSessionForCurrentBookDataClear/, 'app shell must pass AI session reset into task-center side effects after current-book all-data clearing');
assert.match(appTaskCenterHook, /subscribeCurrentBookDataCleared/, 'task center hook must listen to typed current-book clear events across windows');
assert.match(appTaskCenterHook, /resetAiSessionForCurrentBookDataClear/, 'task center hook must clear parent AI answer, citations, diagnostics, and in-flight requests after current-book all-data clearing');
assert.match(settingsDataPanel, /settings\.data\.cleanup\.currentBookAllData/, 'settings center must render a localized current-book all-data destructive action');
assert.match(settingsCenterDataMaintenanceActions, /settings\.dataActions\.clearCurrentBookAllConfirm/, 'current-book all-data confirmation must use the localized confirmation contract');
assert.doesNotMatch(settingsPage, /permanentlyDeleteBook|moveBookToTrash|deleteBookIndex|delete_book_index/, 'current-book all-data clearing must not delete the book, library metadata, or index artifacts');
assert.match(settingsPage, /currentBookTitle/, 'settings center must label current-book cache actions with the active book');
assert.match(app, /currentBookId=\{book\?\.id\}/, 'app shell must pass the active book id into the settings center');
assert.match(settingsPage, /currentBookId\?: string/, 'settings page props must accept the active current book id');
assert.doesNotMatch(settingsPage, /loadCurrentReaderBookId/, 'settings page must not use last-reader localStorage as the current-book action target');
assert.match(readerStorageService, /deleteReaderRecord/, 'reader storage service must support deleting a single reader record');
assert.match(readerStorageService, /deleteAllReaderRecordsByKind/, 'reader storage service must support deleting all reader records by kind');
assert.match(tauriCommands, /delete_reader_record/, 'Tauri commands must expose single reader record deletion');
assert.match(tauriCommands, /delete_reader_records_by_kind/, 'Tauri commands must expose kind-wide reader record deletion');
assert.match(tauriReaderData, /delete_reader_record_in/, 'reader data backend must delete a single reader record');
assert.match(tauriReaderData, /delete_reader_records_by_kind_in/, 'reader data backend must delete all reader records by kind');
assert.match(tauriCommands, /delete_reader_records_by_book/, 'Tauri commands must expose book-scoped reader record deletion');
assert.match(tauriReaderData, /delete_reader_records_by_book_in/, 'reader data backend must delete all reader records for one book');
assert.match(settingsCenterDataMaintenanceActions, /clearOrphanReaderRecordsSettingsData/, 'settings center must provide an orphan reader record cleanup action');
assert.match(settingsCenterDataMaintenanceActions, /cleanupOrphanReaderRecords\(\)/, 'orphan reader cleanup must call the reader storage cleanup service');
assert.match(settingsDataPanel, /settings\.data\.cleanup\.orphanRecords/, 'data cleanup settings must render a localized orphan record cleanup button');
assert.match(settingsCenterDataMaintenanceActions, /settings\.dataActions\.clearOrphansConfirm/, 'orphan cleanup confirmation must use the localized maintenance contract');
assert.match(readerStorageService, /cleanupOrphanReaderRecords/, 'reader storage service must expose orphan reader record cleanup');
assert.match(readerStorageService, /cleanup_orphan_reader_records/, 'reader storage service must invoke the orphan reader record cleanup backend command');
assert.match(tauriCommands, /cleanup_orphan_reader_records_for_library_in/, 'Tauri command layer must clean orphan reader records from full library metadata');
assert.match(tauriCommands, /load_library_records\(data_dir\)/, 'orphan cleanup command must load all library records, including trash entries');
assert.match(tauriCommands, /cleanup_orphan_reader_records/, 'Tauri commands must expose orphan reader record cleanup');
assert.match(tauriReaderData, /cleanup_orphan_reader_records_in/, 'reader data backend must implement orphan reader record cleanup');
assert.match(tauriReaderData, /SELECT DISTINCT book_id[\s\S]*FROM reader_records/, 'orphan cleanup must derive candidates from reader_records book ids');
assert.match(tauriReaderData, /DELETE FROM reader_records WHERE book_id = \?1/, 'orphan cleanup must delete only reader_records rows for orphan book ids');
assert.match(settingsCenterDataMaintenanceActions, /rebuildDatabaseIndexesSettingsData/, 'settings center must provide a database index rebuild maintenance action');
assert.match(settingsCenterDataMaintenanceActions, /rebuildDatabaseIndexes\(\)/, 'database index rebuild settings action must call the task service maintenance command');
assert.match(settingsDataPanel, /settings\.data\.cleanup\.rebuildIndexes/, 'data cleanup settings must render a localized database index rebuild button');
assert.match(settingsCenterDataMaintenanceActions, /settings\.dataActions\.rebuildDbIndexesConfirm/, 'database index rebuild confirmation must use the localized maintenance contract');
assert.match(taskService, /export async function rebuildDatabaseIndexes/, 'task service must expose database index rebuild maintenance');
assert.match(taskService, /rebuild_database_indexes/, 'task service must invoke database index rebuild backend command');
assert.match(tauriCommands, /rebuild_database_indexes/, 'Tauri commands must expose database index rebuild maintenance');
assert.match(tauriCommands, /rebuild_sqlite_database_indexes_in/, 'Tauri command must call SQLite database index maintenance helper');
assert.match(tauriDatabase, /rebuild_sqlite_database_indexes_in/, 'database backend must implement SQLite database index maintenance');
assert.match(databaseIndexMaintenanceBody, /REINDEX[\s\S]*ANALYZE[\s\S]*chunks_fts\(chunks_fts\)[\s\S]*optimize/, 'database index maintenance must run REINDEX, ANALYZE, and FTS optimize');
assert.doesNotMatch(databaseIndexMaintenanceBody, /DELETE FROM (?:chunks|chunks_fts|reader_records)/, 'database index maintenance must not delete chunks, FTS rows, or reader records');
assert.match(settingsCenterDataMaintenanceActions, /vacuumDatabaseSettingsData/, 'settings center must provide a database VACUUM maintenance action');
assert.match(settingsCenterDataMaintenanceActions, /vacuumDatabase\(\)/, 'database VACUUM settings action must call the task service maintenance command');
assert.match(settingsDataPanel, /settings\.data\.cleanup\.vacuum/, 'data cleanup settings must render a localized database VACUUM button');
assert.match(settingsCenterDataMaintenanceActions, /settings\.dataActions\.vacuumDbConfirm/, 'database VACUUM confirmation must use the localized maintenance contract');
assert.match(settingsCenterStatusFormatModel, /formatSettingsByteSize/, 'settings center status format model must format byte sizes readably');
assert.match(settingsCenterDataMaintenanceActions, /formatSettingsByteSize\(payload\.sizeBeforeBytes\)/, 'database VACUUM status must render before and after byte sizes readably');
assert.match(taskService, /export async function vacuumDatabase/, 'task service must expose database VACUUM maintenance');
assert.match(taskService, /vacuum_database/, 'task service must invoke database VACUUM backend command');
assert.match(tauriCommands, /vacuum_database/, 'Tauri commands must expose database VACUUM maintenance');
assert.match(tauriCommands, /vacuum_sqlite_database_in/, 'Tauri command must call SQLite database VACUUM helper');
assert.match(tauriDatabase, /vacuum_sqlite_database_in/, 'database backend must implement SQLite database VACUUM maintenance');
assert.match(databaseVacuumBody, /VACUUM;/, 'database VACUUM maintenance must execute VACUUM');
assert.match(databaseVacuumBody, /size_before_bytes[\s\S]*size_after_bytes[\s\S]*bytes_reclaimed/, 'database VACUUM payload must report before/after/reclaimed bytes');
assert.doesNotMatch(databaseVacuumBody, /DELETE FROM (?:chunks|chunks_fts|reader_records)/, 'database VACUUM maintenance must not delete chunks, FTS rows, or reader records');
assert.match(settingsCenterDataMaintenanceActions, /clearReaderSearchSettingsData/, 'settings center must provide a clear reader search history action');
assert.doesNotMatch(settingsPage, /const readerCacheRecordKinds|const currentBookDataRecordKinds|function dispatchReaderCacheClearedEvents|function dispatchCurrentBookDataClearedEvent|function recordTauriCommandFailure|async function loadDiagnosticsPaths/, 'settings page must not own data maintenance implementation details');
assert.match(settingsCenterService, /searchNormalizeTraditionalChinese:\s*boolean/, 'extended settings must type traditional Chinese search normalization');
assert.match(settingsCenterService, /searchNormalizeNfkc:\s*boolean/, 'extended settings must type NFKC search normalization');
assert.match(settingsCenterService, /searchPinyinInitials:\s*boolean/, 'extended settings must type pinyin-initial search matching');
assert.match(settingsCenterService, /autoIndexImportedBooks:\s*boolean/, 'extended settings must type automatic indexing after book import');
assert.match(settingsCenterService, /autoParseTocOnImport:\s*boolean/, 'extended settings must type automatic TOC parsing after import');
assert.match(settingsCenterService, /autoParseTocOnImport:\s*true/, 'automatic TOC parsing after import must default on');
assert.match(settingsCenterService, /autoParseTocOnImport:\s*typeof settings\?\.autoParseTocOnImport === 'boolean' \? settings\.autoParseTocOnImport : defaultExtendedSettings\.autoParseTocOnImport/, 'extended settings must normalize automatic TOC parsing after import');
assert.match(settingsCenterService, /autoRebuildTocWhenEmpty:\s*boolean/, 'extended settings must type automatic TOC rebuild when reader opens without a usable manifest');
assert.match(settingsCenterService, /autoRebuildTocWhenEmpty:\s*true/, 'automatic TOC rebuild when empty must default on');
assert.match(settingsCenterService, /autoRebuildTocWhenEmpty:\s*typeof settings\?\.autoRebuildTocWhenEmpty === 'boolean' \? settings\.autoRebuildTocWhenEmpty : defaultExtendedSettings\.autoRebuildTocWhenEmpty/, 'extended settings must normalize automatic TOC rebuild when empty');
assert.match(settingsCenterService, /tocRuleChangeRebuildMode:\s*'off' \| 'prompt' \| 'auto'/, 'extended settings must type the TOC rule-change rebuild mode');
assert.match(settingsCenterService, /tocRuleChangeRebuildMode:\s*'prompt'/, 'TOC rule-change rebuild mode must default to prompt');
assert.match(settingsCenterService, /tocRuleChangeRebuildMode:\s*isTocRuleChangeRebuildMode\(settings\?\.tocRuleChangeRebuildMode\) \? settings\.tocRuleChangeRebuildMode : defaultExtendedSettings\.tocRuleChangeRebuildMode/, 'extended settings must normalize the TOC rule-change rebuild mode');
assert.match(settingsCenterService, /previewTocRebuildDiff:\s*boolean/, 'extended settings must type TOC rebuild diff preview');
assert.match(settingsCenterService, /previewTocRebuildDiff:\s*true/, 'TOC rebuild diff preview must default on');
assert.match(settingsCenterService, /previewTocRebuildDiff:\s*typeof settings\?\.previewTocRebuildDiff === 'boolean' \? settings\.previewTocRebuildDiff : defaultExtendedSettings\.previewTocRebuildDiff/, 'extended settings must normalize TOC rebuild diff preview');
assert.match(readerStorageService, /'tocManifest'/, 'reader storage adapter must support persisted TOC manifests');
assert.match(readerStorageService, /'chapterRulesOverride'/, 'reader storage adapter must support per-book chapter rule overrides');
assert.match(tauriReaderData, /"chapterRulesOverride"/, 'reader data backend must accept per-book chapter rule override records');
assert.match(readerChapterParserModel, /export type ReaderTocManifest/, 'reader chapter parser model must type persisted TOC manifests');
assert.match(readerChapterParserModel, /export function buildReaderTocManifest/, 'reader chapter parser model must build TOC manifests from parsed chapters');
assert.match(readerChapterParserModel, /export function summarizeReaderTocManifestDiff/, 'reader chapter parser model must summarize TOC manifest differences before rebuilds');
assert.match(readerChapterParserModel, /export function buildReaderChaptersFromTocManifest/, 'reader chapter parser model must rebuild chapters from a valid TOC manifest');
assert.match(readerModel, /from '\.\/readerChapterParserModel\.js'/, 'reader model must keep compatibility re-exports for reader chapter parser APIs');
assert.match(readerWorkspaceDocument, /loadReaderTocManifest\(book\.id/, 'reader workspace document hook must load persisted TOC manifests');
assert.match(readerWorkspaceDocument, /loadReaderChapterRulesOverride\(book\.id/, 'reader workspace document hook must load per-book chapter rule overrides');
assert.match(readerWorkspaceDocument, /const effectiveChapterRules = useMemo\(\(\) => \(\{ \.\.\.chapterRules, \.\.\.\(bookChapterRulesReadyForReaderBook \? bookChapterRulesOverride \?\? \{\} : \{\}\) \}\)/, 'reader workspace document hook must apply per-book chapter rules only after the active book override has loaded');
assert.match(readerWorkspaceDataActions, /saveReaderRecord\(book\.id,\s*'chapterRulesOverride',\s*override/, 'reader workspace data actions must persist per-book chapter rule overrides');
assert.match(readerWorkspaceDataActions, /saveReaderRecord\(book\.id,\s*'chapterRulesOverride',\s*null/, 'reader workspace data actions must allow clearing per-book chapter rule overrides');
assert.match(readerSettingsPanel, /reader\.settings\.saveBookChapterRulesOverride/, 'reader settings panel must expose saving global chapter rules as the current book override');
assert.match(readerSettingsPanel, /reader\.settings\.clearBookChapterRulesOverride/, 'reader settings panel must expose clearing the current book chapter rule override');
assert.match(readerWorkspaceDocument, /new Worker\(new URL\('\.\.\/\.\.\/features\/reader-core\/readerDocumentWorker\.ts'/, 'reader workspace document hook must offload reader document parsing to the document worker');
assert.match(readerWorkspaceDocument, /tocManifest:\s*tocManifestForReaderBook,\s*chapterParsingOptions,\s*\} satisfies ReaderDocumentWorkerRequest/, 'reader workspace document hook must pass the current-book persisted TOC manifest into the document worker');
assert.match(readerDocumentWorker, /buildReaderChaptersFromTocManifest\(modelBook,\s*tocManifest,\s*chapterParsingOptions\)\s*\?\?\s*buildReaderChapters\(modelBook,\s*chapterParsingOptions\)/, 'reader document worker must reuse valid TOC manifests before parsing from scratch');
assert.match(readerWorkspaceDocument, /rebuildReaderTocManifestIfMissingOrEmpty\(readerBook,\s*sourceChapters,\s*tocManifestForReaderBook,\s*chapterParsingOptions,\s*extendedSettings\.autoRebuildTocWhenEmpty/, 'reader workspace document hook must attempt an automatic TOC manifest rebuild when opening a book with missing or empty TOC data');
assert.match(readerWorkspaceModel, /saveReaderRecord\(book\.id,\s*'tocManifest',\s*nextManifest,\s*'reader-open-toc-rebuild'\)/, 'reader workspace model must persist automatic reader-open TOC rebuilds');
assert.match(readerWorkspaceDocument, /handleChapterRulesChanged\(detail,\s*nextExtendedSettings\)/, 'reader workspace document hook must route chapter-rule settings events through a rule-change rebuild handler');
assert.match(readerWorkspaceDocument, /rebuildReaderTocManifestAfterRuleChange\(readerBook,\s*sourceChapters,\s*tocManifestForReaderBook,\s*chapterParsingOptions,\s*pending\.mode,\s*pending\.previewDiff/, 'reader workspace document hook must rebuild current-book TOC manifests after chapter rules change according to the configured mode and diff-preview setting');
assert.match(readerWorkspaceModel, /saveReaderRecord\(book\.id,\s*'tocManifest',\s*nextManifest,\s*'chapter-rule-change-toc-rebuild'\)/, 'reader workspace model must persist rule-change TOC rebuilds with a distinct source');
assert.match(readerWorkspaceModel, /summarizeReaderTocManifestDiff\(tocManifest,\s*nextManifest\)/, 'reader workspace model must compute a TOC diff summary before prompted rebuilds');
assert.match(readerWorkspaceModel, /formatReaderTocRebuildDiffPrompt\(diffSummary\)/, 'reader workspace model must render a TOC diff preview in prompted rebuilds');
assert.match(appLibraryHook, /shouldRunImportTocParsing\(extendedSettings\.autoParseTocOnImport,\s*\[imported\]\)/, 'library hook must route file imports through the TOC import parsing gate');
assert.match(appLibraryHook, /extendedSettings\.autoIndexImportedBooks[\s\S]*runAppParseAndIndexTasks\('import'/, 'library hook must route imported books through automatic import indexing when enabled');
assert.match(appShellModel, /export function shouldRunImportTocParsing\(autoParseTocOnImport:\s*boolean,\s*imported:\s*Book\[\]\)/, 'app shell model must expose a TOC import parsing gate');
assert.match(appLibraryHook, /await saveImportedBookTocManifests\(\[imported\],\s*chapterRules\)/, 'automatic file-import TOC parsing must persist TOC manifests using current chapter rules');
assert.match(appLibraryHook, /await saveImportedBookTocManifests\(need,\s*chapterRules\)/, 'automatic directory-import TOC parsing must persist TOC manifests using current chapter rules');
assert.match(appLibraryHook, /runAppParseAndIndexTasks\('import',\s*taskContext\)/, 'automatic import indexing must reuse the shared parse/index task runner');
assert.match(appTaskActions, /export async function runAppParseAndIndexTasks\(source:\s*'reader' \| 'import'/, 'app task actions must share parse/index task execution between reader and import workflows');
assert.match(appTaskActions, /getRunTasksUpdatePlan\(nextTasks\)/, 'parse/index task runner must classify nonblocking task-started versus terminal updates');
assert.match(appTaskActions, /dispatchTasksUpdated\(runTasksUpdatePlan\.reason, nextTasks\)/, 'parse/index task runner must dispatch task update events');
assert.match(app, /backgroundTaskRunning/, 'app shell must track nonblocking background tasks without interval polling');
assert.match(app, /backgroundTaskPreviousSnapshotRef/, 'app shell event stream must preserve the task snapshot returned by the nonblocking start command');
assert.match(appTaskCenterHook, /subscribeTaskProgressEvents\(\(events\) => \{/, 'task center hook must subscribe to the Tauri task progress event stream');
assert.match(appTaskActions, /const nextTasks = mergeTaskProgressEventStatus\(previousTasks, eventStatus\)/, 'app task actions must merge progress event payloads into the latest task snapshot');
assert.match(appTaskActions, /const backgroundRunTasksUpdatePlan = getRunTasksUpdatePlan\(nextTasks\)/, 'app task actions must classify terminal task updates');
assert.match(appTaskActions, /context\.setLatestTaskSnapshot\(nextTasks\);[\s\S]*dispatchTasksCompleted\(nextTasks, previousTasks\);[\s\S]*dispatchTasksUpdated\(backgroundRunTasksUpdatePlan\.reason, nextTasks\)/, 'app task actions must advance snapshots before dispatching completion and update events');
assert.doesNotMatch(`${app}\n${appTaskCenterHook}`, /backgroundTaskPollingActive|setBackgroundTaskPollingActive/, 'app shell and task center hook must not keep stale polling state names after switching to event streams');
assert.doesNotMatch(app, /window\.setInterval\([\s\S]*loadTaskStatuses/, 'app shell must not poll task statuses with setInterval after switching to event streams');
assert.match(settingsCenterService, /indexStrategyVersion:\s*'stable' \| 'latest' \| 'compat'/, 'extended settings must type index strategy version');
assert.match(settingsCenterService, /indexChunkSize:\s*string/, 'extended settings must type index chunk size');
assert.match(settingsCenterService, /indexChunkOverlap:\s*string/, 'extended settings must type index chunk overlap');
assert.match(settingsCenterService, /indexRebuildStrategy:\s*'manual' \| 'prompt' \| 'auto'/, 'extended settings must type index rebuild strategy');
assert.match(settingsCenterService, /ftsRepairStrategy:\s*'manual' \| 'prompt' \| 'auto'/, 'extended settings must type FTS repair strategy');
assert.match(settingsCenterService, /indexRecentErrorLimit:\s*string/, 'extended settings must type recent index error retention');
assert.match(settingsCenterService, /indexPauseResumeStrategy:\s*'manual' \| 'ask' \| 'auto'/, 'extended settings must type index pause/resume strategy');
assert.match(settingsSearchPanel, /searchNormalizeTraditionalChinese/, 'settings page must expose traditional Chinese search normalization');
assert.match(settingsSearchPanel, /searchNormalizeNfkc/, 'settings page must expose NFKC search normalization');
assert.match(settingsSearchPanel, /searchPinyinInitials/, 'settings page must expose pinyin-initial search matching');
assert.match(settingsSearchPanel, /autoIndexImportedBooks/, 'search/index settings must expose automatic indexing after book import');
assert.match(settingsChapterPanel, /autoParseTocOnImport/, 'chapter settings must expose automatic TOC parsing after import');
assert.match(settingsChapterPanel, /settings\.chapter\.autoParseTocOnImport\.title/, 'chapter settings panel must expose localized automatic TOC parsing after import');
assert.match(settingsCenterDefaultResetActions, /autoParseTocOnImport:\s*defaultExtendedSettings\.autoParseTocOnImport/, 'chapter defaults must restore automatic TOC parsing after import');
assert.match(settingsChapterPanel, /autoRebuildTocWhenEmpty/, 'chapter settings must expose automatic TOC rebuild when opening a book with empty TOC data');
assert.match(settingsChapterPanel, /settings\.chapter\.autoRebuildTocWhenEmpty\.title/, 'chapter settings panel must expose localized automatic reader-open TOC rebuild');
assert.match(settingsCenterDefaultResetActions, /autoRebuildTocWhenEmpty:\s*defaultExtendedSettings\.autoRebuildTocWhenEmpty/, 'chapter defaults must restore automatic reader-open TOC rebuild');
assert.match(settingsChapterPanel, /tocRuleChangeRebuildMode/, 'chapter settings must expose TOC rebuild behavior after rule changes');
assert.match(settingsChapterPanel, /settings\.chapter\.tocRuleChangeRebuildMode\.title/, 'chapter settings panel must expose localized TOC rebuild behavior after rule changes');
assert.match(settingsCenterDefaultResetActions, /tocRuleChangeRebuildMode:\s*defaultExtendedSettings\.tocRuleChangeRebuildMode/, 'chapter defaults must restore TOC rule-change rebuild mode');
assert.match(settingsChapterPanel, /previewTocRebuildDiff/, 'chapter settings must expose TOC rebuild diff preview');
assert.match(settingsChapterPanel, /settings\.chapter\.previewTocRebuildDiff\.title/, 'chapter settings panel must expose localized TOC rebuild diff preview');
assert.match(settingsCenterDefaultResetActions, /previewTocRebuildDiff:\s*defaultExtendedSettings\.previewTocRebuildDiff/, 'chapter defaults must restore TOC rebuild diff preview');
assert.match(settingsCenterDefaultResetActions, /dispatchSettingsUpdated\(\{ key:\s*'chapterRules',\s*keys:\s*\[[\s\S]*'autoParseTocOnImport'[\s\S]*'autoRebuildTocWhenEmpty'[\s\S]*'tocRuleChangeRebuildMode'[\s\S]*'previewTocRebuildDiff'[\s\S]*\],\s*scope:\s*'chapterRules' \}\)/, 'chapter defaults must dispatch automatic TOC parsing reset as a chapter settings change');
assert.match(settingsSearchPanel, /indexStrategyVersion/, 'search/index settings must expose index strategy version');
assert.match(settingsSearchPanel, /indexChunkSize/, 'search/index settings must expose chunk size');
assert.match(settingsSearchPanel, /indexChunkOverlap/, 'search/index settings must expose chunk overlap');
assert.match(settingsSearchPanel, /indexRebuildStrategy/, 'search/index settings must expose rebuild strategy');
assert.match(settingsSearchPanel, /ftsRepairStrategy/, 'search/index settings must expose FTS repair strategy');
assert.match(settingsPageOptions, /diagnosticPaths\.ftsDatabase/, 'search/index settings must expose the FTS database path');
assert.match(settingsPageOptions, /getPrivacyFilePath\(diagnosticPaths\.ftsDatabase,\s*extendedSettings\)/, 'settings page options hook must render the FTS database path through file-path privacy');
assert.match(settingsSearchPanel, /indexRecentErrorLimit/, 'search/index settings must expose recent index error retention');
assert.match(settingsSearchPanel, /indexPauseResumeStrategy/, 'search/index settings must expose index pause/resume strategy');
assert.match(settingsCenterDefaultResetActions, /parseConcurrency:\s*defaultExtendedSettings\.parseConcurrency/, 'search settings restore defaults must include index concurrency');
assert.match(settingsCenterService, /indexRebuildStrategy:\s*'prompt'/, 'extended settings must default index rebuild strategy to prompt');
assert.match(settingsCenterService, /indexRecentErrorLimit:\s*'5'/, 'extended settings must default recent index error retention');
assert.match(settingsCenterService, /indexPauseResumeStrategy:\s*'manual'/, 'extended settings must default index pause/resume strategy to manual');
assert.match(settingsCenterService, /indexRebuildStrategy:\s*isIndexRebuildStrategy\(settings\?\.indexRebuildStrategy\)/, 'extended settings must normalize index rebuild strategy');
assert.match(settingsCenterService, /indexRecentErrorLimit:\s*clampNumericString\(settings\?\.indexRecentErrorLimit,\s*defaultExtendedSettings\.indexRecentErrorLimit,\s*0,\s*50\)/, 'extended settings must clamp recent index error retention');
assert.match(settingsCenterService, /indexPauseResumeStrategy:\s*isIndexPauseResumeStrategy\(settings\?\.indexPauseResumeStrategy\)/, 'extended settings must normalize index pause/resume strategy');
assert.match(settingsCenterDefaultResetActions, /indexRebuildStrategy:\s*defaultExtendedSettings\.indexRebuildStrategy/, 'search settings restore defaults must include index rebuild strategy');
assert.match(settingsCenterDefaultResetActions, /indexRecentErrorLimit:\s*defaultExtendedSettings\.indexRecentErrorLimit/, 'search settings restore defaults must include recent index error retention');
assert.match(settingsCenterDefaultResetActions, /indexPauseResumeStrategy:\s*defaultExtendedSettings\.indexPauseResumeStrategy/, 'search settings restore defaults must include index pause/resume strategy');
assert.match(settingsCenterDefaultResetActions, /'parseConcurrency'/, 'search settings restore key list must include index concurrency');
assert.match(appTaskCenterHook, /getStartupTaskResumePlan\(s\?\.tasks \?\? \[\], extendedSettings\.indexPauseResumeStrategy\)[\s\S]*resumeStartupTasks\(s\?\.tasks \?\? \[\], extendedSettings\.indexPauseResumeStrategy/, 'task center hook must delegate startup task-resume behavior to the configured strategy');
assert.match(appTaskActions, /resumePausedTasksAndRun/, 'auto pause/resume startup path must requeue paused tasks before running');
assert.match(taskService, /export async function resumePausedTasksAndRun\(taskIds: string\[\]\)[\s\S]*await retryTask\(taskId\)[\s\S]*runParseAndIndexTasks\(\)/, 'task service must resume paused tasks before running queued tasks');
assert.match(taskCenterState, /loadExtendedSettings\(\)\.ftsRepairStrategy/, 'task center must read the configured FTS repair strategy');
assert.match(taskCenterState, /ftsRepairStrategy !== 'auto'[\s\S]*await confirm\(t\('tasks\.confirm\.repairFts'\)\)/, 'manual and prompt FTS repair strategies must keep user confirmation');
assert.match(taskCenterState, /applyAutoFtsRepairStrategy[\s\S]*book\.chunkCount > 0 && book\.ftsRowCount === 0[\s\S]*repairBookFts\(bookId\)/, 'auto FTS repair strategy must repair diagnostics entries with missing FTS rows');
assert.match(settingsCenterModel, /export const settingsScopeMatrix/, 'settings center model must define a settings scope matrix');
assert.match(settingsPagePropsBuilders, /settingsScopeMatrix/, 'settings page prop builders must render the shared settings scope matrix');
assert.match(settingsDataPanel, /settings-scope-matrix/, 'settings center must render a settings scope matrix');
assert.match(settingsCenterModel, /scope:\s*'global'/, 'settings scope matrix must include global settings');
assert.match(settingsCenterModel, /scope:\s*'book'/, 'settings scope matrix must include per-book settings');
assert.match(settingsCenterModel, /scope:\s*'session'/, 'settings scope matrix must include session settings');
assert.doesNotMatch(settingsChapterPanel, /<SettingControl title="英文 Part \/ Book \/ Volume"|<SettingControl title="短标题编号"|<SettingControl title="短副标题"|<SettingControl title="标题必须独占一行"|valueText="已支持"|valueText="已强制"/, 'chapter settings must not expose parser implementation status as ordinary settings rows');
assert.match(settingsChapterPanel, /forbiddenHeadingPunctuation/, 'chapter settings must render editable forbidden heading punctuation');
assert.match(settingsChapterPanel, /forbiddenHeadingStartChars/, 'chapter settings must render editable forbidden heading start characters');
assert.match(settingsChapterPanel, /customRegexRules/, 'chapter settings must render a custom regex rule list');
assert.match(settingsChapterPanel, /addChapterRegexRule/, 'chapter settings must allow adding custom regex rules');
assert.match(settingsChapterPanel, /moveChapterRegexRule/, 'chapter settings must allow changing custom regex rule priority order');
assert.match(settingsChapterPanel, /chapterRegexTestInput/, 'chapter settings must expose a regex rule test input');
assert.match(settingsChapterPanel, /matchReaderChapterRegexRule/, 'chapter settings must preview custom regex matches with the reader parser helper');
assert.match(settingsChapterPanel, /minHeadingConfidence/, 'chapter settings must expose an editable heading confidence threshold');
assert.match(settingsChapterPanel, /autoIgnoreAdHeadings/, 'chapter settings must expose automatic ad heading filtering');
assert.match(settingsChapterPanel, /autoIgnoreRepeatedTocHeadings/, 'chapter settings must expose automatic repeated TOC page filtering');
assert.match(settingsChapterPanel, /compactHeadingSuffixLength/, 'chapter settings must expose compact heading suffix length');
assert.match(settingsChapterPanel, /preserveCompactPrefixText/, 'chapter settings must expose whether compact split keeps prefix text');
assert.match(settingsChapterPanel, /compactSplitPreviewInput/, 'chapter settings must expose compact split preview input');
assert.match(settingsChapterPanel, /previewReaderCompactChapterSplit/, 'chapter settings must render compact split preview through the reader parser helper');
assert.match(settingsPage, /chapterParseDiagnostics/, 'chapter settings must compute parser diagnostics from current chapter rules');
assert.match(settingsCenterChapterDiagnosticsModel, /buildChapterParsingOptions/, 'settings center chapter diagnostics model must map chapter rules into parser options');
assert.match(settingsCenterChapterDiagnosticsModel, /createChapterDiagnosticsSampleBook/, 'settings center chapter diagnostics model must build the diagnostics sample book');
assert.match(settingsPageOptions, /buildChapterParsingOptions\(chapterRules\)/, 'settings page options hook must delegate parser option mapping to the chapter diagnostics model');
assert.match(settingsPageOptions, /createChapterDiagnosticsSampleBook\(chapterDiagnosticsCleanedContent\)/, 'settings page options hook must delegate diagnostics sample book construction to the chapter diagnostics model');
assert.match(settingsChapterPanel, /chapterDiagnosticsSampleInput/, 'chapter settings must expose a sample input for parser diagnostics');
assert.match(settingsPageOptions, /buildReaderChapterDiagnostics/, 'chapter settings must render parser diagnostics through the reader parser helper');
assert.match(settingsChapterPanel, /copyChapterParseDiagnostics/, 'chapter settings must expose one-click copy for parser diagnostics');
assert.match(settingsChapterPanel, /settings-chapter-diagnostics/, 'chapter settings must render a dedicated parser diagnostics block');
assert.match(settingsChapterPanel, /settings-chapter-preview-list/, 'chapter settings must render a final TOC preview list');
assert.match(settingsChapterPanel, /chapterPreview/, 'chapter parser diagnostics must expose final chapter preview entries to the settings page');
assert.match(settingsChapterPanel, /settings\.chapter\.diagnostics\.startLine/, 'chapter rule preview must expose localized parsed chapter start lines');
assert.match(settingsChapterPanel, /settings\.chapter\.diagnostics\.paragraphCount/, 'chapter rule preview must expose localized parsed chapter paragraph counts');
assert.match(styles, /\.settings-chapter-preview-list/, 'settings styles must define the chapter rule preview list');
assert.match(styles, /\.settings-chapter-preview-row/, 'settings styles must define compact chapter preview rows');
assert.match(styles, /@media \(max-width: 1100px\)[\s\S]*\.settings-chapter-preview-row \{ grid-template-columns: minmax\(0, 1fr\) minmax\(0, 1fr\); \}/, 'chapter rule preview rows must collapse metadata before overflowing narrow settings columns');
assert.match(styles, /@media \(max-width: 720px\)[\s\S]*\.settings-chapter-preview-row \{ grid-template-columns: 1fr; \}/, 'chapter rule preview rows must collapse to one column on very narrow settings viewports');
assert.match(settingsChapterPanel, /parserVersion/, 'chapter parser diagnostics must show the parser version');
assert.match(settingsChapterPanel, /hashManifest/, 'chapter parser diagnostics must show the chapter hash manifest');
assert.match(settingsChapterPanel, /ruleMatches/, 'chapter parser diagnostics must show matched parser rules');
assert.match(settingsChapterPanel, /unmatchedSamples/, 'chapter parser diagnostics must show unmatched samples and reasons');
assert.match(settingsChapterPanel, /shortChapters/, 'chapter parser diagnostics must show abnormal short chapters');
assert.match(settingsChapterPanel, /longChapters/, 'chapter parser diagnostics must show abnormal long chapters');
assert.match(settingsChapterPanel, /autoDetectBookTitle/, 'chapter settings must expose automatic book title detection');
assert.match(settingsChapterPanel, /bookTitleBracketMode/, 'chapter settings must expose book title bracket mode');
assert.match(settingsChapterPanel, /bookTitleMaxLength/, 'chapter settings must expose book title max length');
assert.match(settingsChapterPanel, /firstLineAsBookTitle/, 'chapter settings must expose first-line title detection');
assert.match(settingsChapterPanel, /inferBookTitleFromFileName/, 'chapter settings must expose filename title inference');
assert.match(settingsChapterPanel, /paragraphMode/, 'chapter settings must expose paragraph mode');
assert.match(settingsChapterPanel, /shortLineMergeThreshold/, 'chapter settings must expose short-line merge threshold');
assert.match(settingsChapterPanel, /longParagraphSliceSize/, 'chapter settings must expose long paragraph slice size');
assert.match(readerChapterParserModel, /(?:Chapter\|Part\|Book\|Volume)/, 'reader chapter parser model must support English Part, Book, and Volume headings');
assert.match(readerChapterParserModel, /SHORT_CHAPTER_HEADING_RE/, 'reader chapter parser model must support numeric and Chinese short headings');
assert.match(readerChapterParserModel, /forbiddenHeadingPunctuation/, 'reader chapter parser model must accept configurable forbidden heading punctuation');
assert.match(readerChapterParserModel, /forbiddenHeadingStartChars/, 'reader chapter parser model must accept configurable forbidden heading start characters');
assert.match(readerModel, /matchReaderChapterRegexRule/, 'reader model must expose custom regex rule matching for settings preview');
assert.match(readerChapterParserModel, /customRegexRules/, 'reader chapter parser model must consume custom regex rule lists');
assert.match(readerChapterParserModel, /enabled\?:\s*boolean/, 'reader chapter parser model must type the chapter detection master switch');
assert.match(readerChapterParserModel, /enableChineseChapter\?:\s*boolean/, 'reader chapter parser model must type the Chinese chapter rule switch');
assert.match(readerChapterParserModel, /enableChineseVolume\?:\s*boolean/, 'reader chapter parser model must type the Chinese volume rule switch');
assert.match(readerChapterParserModel, /enableSpecialHeadings\?:\s*boolean/, 'reader chapter parser model must type the special heading rule switch');
assert.match(readerChapterParserModel, /enableEnglishChapter\?:\s*boolean/, 'reader chapter parser model must type the English chapter rule switch');
assert.match(readerChapterParserModel, /enableCompactSplit\?:\s*boolean/, 'reader chapter parser model must type the compact chapter split switch');
assert.match(readerChapterParserModel, /if \(options\.enabled === false\) return false/, 'reader chapter parser model must honor the chapter detection master switch');
assert.match(readerChapterParserModel, /options\.enableChineseChapter !== false && CHINESE_CHAPTER_HEADING_RE\.test/, 'reader chapter parser model must honor the Chinese chapter rule switch');
assert.match(readerChapterParserModel, /options\.enableChineseVolume === false/, 'reader chapter parser model must honor the Chinese volume rule switch');
assert.match(readerChapterParserModel, /options\.enableSpecialHeadings !== false && SPECIAL_CHAPTER_HEADING_RE\.test/, 'reader chapter parser model must honor the special heading rule switch');
assert.match(readerChapterParserModel, /options\.enableEnglishChapter !== false && ENGLISH_CHAPTER_HEADING_RE\.test/, 'reader chapter parser model must honor the English chapter rule switch');
assert.match(readerChapterParserModel, /options\.enabled === false \|\| options\.enableCompactSplit === false/, 'reader chapter parser model must honor the compact chapter split switch');
assert.match(readerModel, /getReaderChapterHeadingConfidence/, 'reader model must expose chapter heading confidence scoring for compatibility');
assert.match(readerChapterParserModel, /minHeadingConfidence/, 'reader chapter parser model must consume the heading confidence threshold');
assert.match(readerChapterParserModel, /autoIgnoreAdHeadings/, 'reader chapter parser model must consume automatic ad heading filtering');
assert.match(readerChapterParserModel, /autoIgnoreRepeatedTocHeadings/, 'reader chapter parser model must consume automatic repeated TOC page filtering');
assert.match(readerChapterParserModel, /compactHeadingSuffixLength/, 'reader chapter parser model must consume compact heading suffix length');
assert.match(readerChapterParserModel, /preserveCompactPrefixText/, 'reader chapter parser model must consume compact prefix preservation');
assert.match(readerModel, /previewReaderCompactChapterSplit/, 'reader model must expose compact split preview');
assert.match(readerModel, /buildReaderChapterDiagnostics/, 'reader model must expose parser diagnostics for settings preview');
assert.match(readerChapterParserModel, /parserVersion:\s*String\(parserVersion\)/, 'reader parser diagnostics must derive parser version from the parser constant');
assert.match(readerChapterParserModel, /buildReaderChapterHashManifest\(chapters\)/, 'reader parser diagnostics must include chapter hash manifests');
assert.match(readerChapterParserModel, /ruleMatches/, 'reader parser diagnostics must include matched parser rules');
assert.match(readerChapterParserModel, /unmatchedSamples/, 'reader parser diagnostics must include unmatched heading samples');
assert.match(styles, /settings-chapter-diagnostics/, 'settings styles must define the chapter diagnostics block');
assert.match(readerModel, /resolveReaderBookTitle/, 'reader model must expose configurable book title resolution for compatibility');
assert.match(readerChapterParserModel, /paragraphMode/, 'reader chapter parser model must consume paragraph mode');
assert.match(readerChapterParserModel, /'chinese-reflow'/, 'reader chapter parser model must support Chinese paragraph reflow mode');
assert.match(readerChapterParserModel, /joinChineseReflowLine/, 'reader chapter parser model must join hard-wrapped Chinese paragraph lines');
assert.match(readerChapterParserModel, /shortLineMergeThreshold/, 'reader chapter parser model must consume short-line merge threshold');
assert.match(readerChapterParserModel, /estimateLongParagraphSlices\(text: string, size = 1200\)/, 'reader chapter parser model must keep configurable long paragraph slicing');
assert.match(readerChapterParserModel, /estimateLongParagraphSlices\(paragraph,\s*size\)/, 'reader chapter parser model must use configurable long paragraph slicing when building paragraphs');
assert.match(readerWorkspaceModel, /autoDetectBookTitle/, 'reader workspace model must pass book title detection settings into parsing');
assert.match(readerWorkspace, /tocEditUndoStack/, 'reader workspace must keep a TOC edit undo stack for split error rollback');
assert.match(readerPage, /onUndoTocEdit/, 'reader page must expose TOC undo for split error rollback');
assert.match(readerWorkspaceState, /loadChapterRules/, 'reader workspace state hook must load chapter rules from the settings center');
assert.match(readerWorkspaceModel, /removeAds:\s*chapterRules\.removeAds/, 'reader workspace model must pass TXT ad cleanup settings into content cleanup');
assert.match(readerWorkspaceModel, /adKeywords,\s*removeAdUrls:\s*chapterRules\.removeAdUrls,\s*removePaginationNoise:\s*chapterRules\.removePaginationNoise/, 'reader workspace model must pass granular ad cleanup settings into content cleanup');
assert.match(readerWorkspaceModel, /function createReaderCleanupOptions\(\s*chapterRules:\s*ChapterRuleDraft,\s*preserveBlankLines:\s*boolean\s*\)/, 'reader workspace model must route TXT cleanup options through a helper that receives preserve-original-blank-lines');
assert.match(readerWorkspaceModel, /normalizeBlankLines:\s*chapterRules\.normalizeBlankLines && !preserveBlankLines/, 'reader workspace model must combine TXT blank-line normalization with preserve-original-blank-lines');
assert.match(readerWorkspaceModel, /trimTrailingWhitespace:\s*chapterRules\.trimTrailingWhitespace/, 'reader workspace model must pass trailing whitespace cleanup settings into content cleanup');
assert.match(readerWorkspaceModel, /normalizeFullWidthSpaces:\s*chapterRules\.normalizeFullWidthSpaces/, 'reader workspace model must pass full-width space cleanup settings into content cleanup');
assert.match(readerWorkspaceModel, /customCleanupRules:\s*chapterRules\.customCleanupRules/, 'reader workspace model must pass custom TXT cleanup rules into content cleanup');
assert.match(readerWorkspaceDocument, /cleanupOptions:\s*createReaderCleanupOptions\(effectiveChapterRules,\s*settings\.preserveBlankLines\),\s*tocManifest:\s*tocManifestForReaderBook,\s*chapterParsingOptions,\s*\} satisfies ReaderDocumentWorkerRequest/, 'reader workspace document hook must pass cleanup options and current-book TOC filters into the document worker');
assert.match(readerDocumentWorker, /buildReaderChapters\(modelBook,\s*chapterParsingOptions\)/, 'reader document worker must pass chapter rule filters into chapter parsing');
assert.match(readerWorkspaceModel, /enabled:\s*chapterRules\.enabled/, 'reader workspace model must pass the chapter detection master switch into parsing');
assert.match(readerWorkspaceModel, /enableChineseChapter:\s*chapterRules\.enableChineseChapter/, 'reader workspace model must pass the Chinese chapter rule switch into parsing');
assert.match(readerWorkspaceModel, /enableChineseVolume:\s*chapterRules\.enableChineseVolume/, 'reader workspace model must pass the Chinese volume rule switch into parsing');
assert.match(readerWorkspaceModel, /enableSpecialHeadings:\s*chapterRules\.enableSpecialHeadings/, 'reader workspace model must pass the special heading rule switch into parsing');
assert.match(readerWorkspaceModel, /enableEnglishChapter:\s*chapterRules\.enableEnglishChapter/, 'reader workspace model must pass the English chapter rule switch into parsing');
assert.match(readerWorkspaceModel, /enableCompactSplit:\s*chapterRules\.enableCompactSplit/, 'reader workspace model must pass the compact split switch into parsing');
assert.match(readerWorkspaceModel, /customRegexRules:\s*chapterRules\.customRegexRules/, 'reader workspace model must pass custom regex rules into chapter parsing');
assert.match(appShellModel, /enabled:\s*chapterRules\.enabled/, 'import TOC parsing must pass the chapter detection master switch');
assert.match(appShellModel, /enableChineseChapter:\s*chapterRules\.enableChineseChapter/, 'import TOC parsing must pass the Chinese chapter rule switch');
assert.match(appShellModel, /enableChineseVolume:\s*chapterRules\.enableChineseVolume/, 'import TOC parsing must pass the Chinese volume rule switch');
assert.match(appShellModel, /enableSpecialHeadings:\s*chapterRules\.enableSpecialHeadings/, 'import TOC parsing must pass the special heading rule switch');
assert.match(appShellModel, /enableEnglishChapter:\s*chapterRules\.enableEnglishChapter/, 'import TOC parsing must pass the English chapter rule switch');
assert.match(appShellModel, /enableCompactSplit:\s*chapterRules\.enableCompactSplit/, 'import TOC parsing must pass the compact split switch');
assert.match(appShellModel, /customCleanupRules:\s*chapterRules\.customCleanupRules/, 'import TXT cleanup must pass custom cleanup rules to the backend');
assert.match(settingsCenterChapterDiagnosticsModel, /enabled:\s*chapterRules\.enabled/, 'settings parser diagnostics must pass the chapter detection master switch');
assert.match(settingsCenterChapterDiagnosticsModel, /enableChineseChapter:\s*chapterRules\.enableChineseChapter/, 'settings parser diagnostics must pass the Chinese chapter rule switch');
assert.match(settingsCenterChapterDiagnosticsModel, /enableChineseVolume:\s*chapterRules\.enableChineseVolume/, 'settings parser diagnostics must pass the Chinese volume rule switch');
assert.match(settingsCenterChapterDiagnosticsModel, /enableSpecialHeadings:\s*chapterRules\.enableSpecialHeadings/, 'settings parser diagnostics must pass the special heading rule switch');
assert.match(settingsCenterChapterDiagnosticsModel, /enableEnglishChapter:\s*chapterRules\.enableEnglishChapter/, 'settings parser diagnostics must pass the English chapter rule switch');
assert.match(settingsCenterChapterDiagnosticsModel, /enableCompactSplit:\s*chapterRules\.enableCompactSplit/, 'settings parser diagnostics must pass the compact split switch');
assert.match(settingsCenterSnapshotActions, /dispatchSettingsUpdated\(\{ key:\s*'settingsImport',\s*keys:\s*Object\.keys\(snapshot\.chapterRules\),\s*scope:\s*'chapterRules' \}\)/, 'settings import must broadcast imported chapter rules to open readers');
assert.match(settingsCenterService, /forbiddenHeadingPunctuation:\s*string/, 'chapter rules must type editable forbidden punctuation');
assert.match(settingsCenterService, /forbiddenHeadingStartChars:\s*string/, 'chapter rules must type editable forbidden start characters');
assert.match(settingsCenterService, /export type ChapterRegexRuleDraft/, 'chapter rules must type custom regex rule entries');
assert.match(settingsCenterService, /customRegexRules:\s*ChapterRegexRuleDraft\[\]/, 'chapter rules must include a custom regex rule list');
assert.match(settingsCenterService, /minHeadingConfidence:\s*number/, 'chapter rules must type the heading confidence threshold');
assert.match(settingsCenterService, /autoIgnoreAdHeadings:\s*boolean/, 'chapter rules must type automatic ad heading filtering');
assert.match(settingsCenterService, /autoIgnoreRepeatedTocHeadings:\s*boolean/, 'chapter rules must type automatic repeated TOC page filtering');
assert.match(settingsCenterService, /compactHeadingSuffixLength:\s*number/, 'chapter rules must type compact heading suffix length');
assert.match(settingsCenterService, /preserveCompactPrefixText:\s*boolean/, 'chapter rules must type compact prefix preservation');
assert.match(settingsCenterService, /autoDetectBookTitle:\s*boolean/, 'chapter rules must type automatic book title detection');
assert.match(settingsCenterService, /bookTitleBracketMode:\s*'book-title' \| 'angle' \| 'both' \| 'custom'/, 'chapter rules must type book title bracket mode');
assert.match(settingsCenterService, /bookTitleMaxLength:\s*number/, 'chapter rules must type book title max length');
assert.match(settingsCenterService, /firstLineAsBookTitle:\s*boolean/, 'chapter rules must type first-line title detection');
assert.match(settingsCenterService, /inferBookTitleFromFileName:\s*boolean/, 'chapter rules must type filename title inference');
assert.match(settingsCenterService, /paragraphMode:\s*'line' \| 'blank-line' \| 'merge-short-lines'/, 'chapter rules must type paragraph mode');
assert.match(settingsCenterService, /'chinese-reflow'/, 'chapter rules must allow Chinese paragraph reflow mode');
assert.match(settingsCenterService, /shortLineMergeThreshold:\s*number/, 'chapter rules must type short-line merge threshold');
assert.match(settingsCenterService, /longParagraphSliceSize:\s*number/, 'chapter rules must type long paragraph slice size');
assert.match(settingsCenterService, /trimTrailingWhitespace:\s*boolean/, 'chapter rules must type trailing whitespace cleanup');
assert.match(settingsCenterService, /normalizeFullWidthSpaces:\s*boolean/, 'chapter rules must type full-width space cleanup');
assert.match(settingsCenterService, /export type CustomCleanupRuleDraft/, 'chapter rules must type custom cleanup rule entries');
assert.match(settingsCenterService, /customCleanupRules:\s*CustomCleanupRuleDraft\[\]/, 'chapter rules must include custom TXT cleanup rule list');
assert.match(settingsCenterService, /adKeywords:\s*string/, 'chapter rules must type ad keyword lists');
assert.match(settingsCenterService, /removeAdUrls:\s*boolean/, 'chapter rules must type ad URL cleanup');
assert.match(settingsCenterService, /removePaginationNoise:\s*boolean/, 'chapter rules must type pagination noise cleanup');
assert.match(settingsCenterService, /removeAds:\s*typeof settings\?\.removeAds === 'boolean' \? settings\.removeAds : defaultChapterRules\.removeAds/, 'chapter rules must normalize ad cleanup as a boolean');
assert.match(settingsCenterService, /adKeywords:\s*normalizeAdKeywords\(settings\?\.adKeywords\)/, 'chapter rules must normalize ad keyword lists');
assert.match(settingsCenterService, /removeAdUrls:\s*typeof settings\?\.removeAdUrls === 'boolean' \? settings\.removeAdUrls : defaultChapterRules\.removeAdUrls/, 'chapter rules must normalize ad URL cleanup as a boolean');
assert.match(settingsCenterService, /removePaginationNoise:\s*typeof settings\?\.removePaginationNoise === 'boolean' \? settings\.removePaginationNoise : defaultChapterRules\.removePaginationNoise/, 'chapter rules must normalize pagination noise cleanup as a boolean');
assert.match(settingsCenterService, /normalizeBlankLines:\s*typeof settings\?\.normalizeBlankLines === 'boolean' \? settings\.normalizeBlankLines : defaultChapterRules\.normalizeBlankLines/, 'chapter rules must normalize blank-line cleanup as a boolean');
assert.match(settingsCenterService, /trimTrailingWhitespace:\s*typeof settings\?\.trimTrailingWhitespace === 'boolean' \? settings\.trimTrailingWhitespace : defaultChapterRules\.trimTrailingWhitespace/, 'chapter rules must normalize trailing whitespace cleanup as a boolean');
assert.match(settingsCenterService, /normalizeFullWidthSpaces:\s*typeof settings\?\.normalizeFullWidthSpaces === 'boolean' \? settings\.normalizeFullWidthSpaces : defaultChapterRules\.normalizeFullWidthSpaces/, 'chapter rules must normalize full-width space cleanup as a boolean');
assert.match(settingsCenterService, /customCleanupRules:\s*normalizeCustomCleanupRules\(settings\?\.customCleanupRules\)/, 'chapter rules must normalize custom TXT cleanup rule lists');
assert.match(settingsChapterPanel, /settings\.chapter\.adKeywords\.title/, 'settings page must expose localized ad keyword lists');
assert.match(settingsChapterPanel, /settings\.chapter\.customCleanupRules\.title/, 'settings page must expose localized custom TXT cleanup rules');
assert.match(settingsChapterPanel, /addCustomCleanupRule/, 'settings page must allow adding custom TXT cleanup rules');
assert.match(settingsChapterPanel, /settings-custom-cleanup-rule-list/, 'custom TXT cleanup rules must use a dedicated bounded list container');
assert.match(settingsChapterPanel, /settings-custom-cleanup-rule-scroll/, 'custom TXT cleanup rules must place rule rows inside a scroll container');
assert.match(settingsChapterPanel, /customCleanupRuleImportInputRef/, 'settings page must provide an import input for custom TXT cleanup rules');
assert.match(settingsChapterPanel, /exportCustomCleanupRulesFromSettings/, 'settings page must export custom TXT cleanup rules');
assert.match(settingsChapterPanel, /importCustomCleanupRulesFromSettings/, 'settings page must import custom TXT cleanup rules');
assert.match(settingsCenterImportModel, /buildCustomCleanupRulesExportPayload/, 'settings center import model must build custom TXT cleanup rule export payloads');
assert.match(settingsCenterImportModel, /parseCustomCleanupRulesImportPayload/, 'settings center import model must parse custom TXT cleanup rule import payloads');
assert.match(settingsCenterChapterActions, /updateCustomCleanupRules\(importedRules, t\('settings\.chapterActions\.cleanupRulesImported', \{ count: importedRules\.length \}\)\)/, 'custom TXT cleanup rule import must only update the custom cleanup rule list');
assert.match(settingsChapterPanel, /chapterToggle\('removeAdUrls', tr\('settings\.chapter\.removeAdUrls\.title'\)\)/, 'settings page must expose localized ad URL cleanup');
assert.match(settingsChapterPanel, /chapterToggle\('removePaginationNoise', tr\('settings\.chapter\.removePaginationNoise\.title'\)\)/, 'settings page must expose localized pagination noise cleanup');
assert.match(settingsChapterPanel, /chapterToggle\('trimTrailingWhitespace', tr\('settings\.chapter\.trimTrailingWhitespace\.title'\)\)/, 'settings page must expose localized trailing whitespace cleanup');
assert.match(settingsChapterPanel, /chapterToggle\('normalizeFullWidthSpaces', tr\('settings\.chapter\.normalizeFullWidthSpaces\.title'\)\)/, 'settings page must expose localized full-width space cleanup');
assert.match(settingsPage, /txtCleanupPreviewInput/, 'settings page must expose TXT cleanup preview input');
assert.match(settingsPage, /txtCleanupPreviewOutput/, 'settings page must compute TXT cleanup preview output');
assert.match(settingsPageOptions, /cleanTxtContent\(txtCleanupPreviewInput/, 'settings page options hook must use reader cleanup helper for TXT cleanup preview');
assert.match(settingsPageOptions, /customCleanupRules:\s*chapterRules\.customCleanupRules/, 'settings cleanup preview must pass custom TXT cleanup rules');
assert.match(settingsChapterPanel, /settings-cleanup-preview/, 'settings page must render a dedicated cleanup preview block');
assert.match(styles, /settings-cleanup-preview/, 'settings styles must define the TXT cleanup preview block');
assert.match(settingsCenterService, /forbiddenHeadingPunctuation:\s*'。；;：、'/, 'chapter rules must default to the current forbidden punctuation');
assert.match(settingsCenterService, /forbiddenHeadingStartChars:\s*'\(\[（【\['/, 'chapter rules must default to the current forbidden start characters');
assert.match(settingsCenterService, /normalizeChapterRegexRules/, 'chapter rules must normalize custom regex rule lists');
assert.match(settingsCenterService, /normalizeCustomCleanupRules/, 'chapter rules must normalize custom TXT cleanup rule lists');
assert.match(settingsCenterService, /minHeadingConfidence:\s*Number\(clampNumericString/, 'chapter rules must clamp the heading confidence threshold');
assert.match(settingsCenterService, /normalizeChapterRuleText/, 'chapter rules must normalize editable text filter fields');
assert.match(settingsCenterModel, /scope:\s*'derived'/, 'settings scope matrix must include derived settings');
assert.match(settingsPage, /initialGroup/, 'settings page must support opening a specific settings group');
assert.match(settingsCenterService, /export type ExtendedSettings/, 'extended settings type must live in a shared service');
assert.match(settingsCenterService, /loadExtendedSettings/, 'shared service must expose extended settings loading');
assert.match(settingsCenterService, /saveExtendedSettings/, 'shared service must expose extended settings saving');
assert.match(settingsCenterService, /customThemeColor:\s*string/, 'extended settings must type the custom theme color');
assert.match(settingsCenterService, /customThemeColor:\s*'#4d5d86'/, 'extended settings must default the custom theme color');
assert.match(settingsCenterService, /normalizeHexColor\(settings\?\.customThemeColor,\s*defaultExtendedSettings\.customThemeColor\)/, 'extended settings must normalize custom theme colors');
assert.match(settingsCenterService, /multiWindowConflictStrategy:\s*'latest' \| 'current-window' \| 'manual'/, 'extended settings must type the multi-window conflict strategy');
assert.match(settingsCenterService, /readerProgressMode:\s*'chapters' \| 'characters' \| 'pages'/, 'extended settings must type the reader progress calculation mode');
assert.match(settingsCenterService, /tocShowVolumeHierarchy:\s*boolean/, 'extended settings must type TOC volume hierarchy visibility');
assert.match(settingsCenterService, /tocVolumeClickable:\s*boolean/, 'extended settings must type clickable volume headings');
assert.match(settingsCenterService, /tocShowChapterNumbers:\s*boolean/, 'extended settings must type TOC chapter number visibility');
assert.match(settingsCenterService, /tocCollapseVolumes:\s*boolean/, 'extended settings must type TOC volume collapsing');
assert.match(settingsCenterService, /tocExpandActiveVolume:\s*boolean/, 'extended settings must type automatic active-volume expansion');
assert.match(settingsCenterService, /tocShowChapterWordCount:\s*boolean/, 'extended settings must type TOC chapter word count visibility');
assert.match(settingsCenterService, /tocShowChapterProgress:\s*boolean/, 'extended settings must type TOC chapter progress visibility');
assert.match(settingsCenterService, /tocTitleGroupingEnabled:\s*boolean/, 'extended settings must type automatic TOC title keyword grouping');
assert.match(settingsCenterService, /tocTitleGroupKeywords:\s*string/, 'extended settings must type TOC title group keywords');
assert.match(settingsCenterService, /export type TocTitleGroupRuleDraft/, 'extended settings must type custom TOC title group rules');
assert.match(settingsCenterService, /tocTitleGroupRules:\s*TocTitleGroupRuleDraft\[\]/, 'extended settings must include custom TOC title group rules');
assert.match(settingsCenterService, /normalizeTocTitleGroupRules/, 'extended settings must normalize custom TOC title group rules');
assert.match(settingsChapterPanel, /tocShowVolumeHierarchy/, 'settings page must expose TOC volume hierarchy visibility');
assert.match(settingsChapterPanel, /tocVolumeClickable/, 'settings page must expose clickable volume headings');
assert.match(settingsChapterPanel, /tocShowChapterNumbers/, 'settings page must expose TOC chapter number visibility');
assert.match(settingsChapterPanel, /tocCollapseVolumes/, 'settings page must expose TOC volume collapsing');
assert.match(settingsChapterPanel, /tocExpandActiveVolume/, 'settings page must expose automatic active-volume expansion');
assert.match(settingsChapterPanel, /tocShowChapterWordCount/, 'settings page must expose TOC chapter word count visibility');
assert.match(settingsChapterPanel, /tocShowChapterProgress/, 'settings page must expose TOC chapter progress visibility');
assert.match(settingsChapterPanel, /tocTitleGroupingEnabled/, 'settings page must expose automatic TOC title keyword grouping');
assert.match(settingsChapterPanel, /tocTitleGroupKeywords/, 'settings page must expose TOC title group keywords');
assert.match(settingsChapterPanel, /addTocTitleGroupRule/, 'settings page must allow adding custom TOC title group rules');
assert.match(settingsChapterPanel, /tocTitleGroupRules/, 'settings page must expose custom TOC title group rules');
assert.match(styles, /\.settings-custom-cleanup-rule-scroll\s*\{[\s\S]*max-height:\s*calc\([^}]*--settings-cleanup-rule-visible-count[^}]*\)[\s\S]*overflow:\s*auto/, 'custom TXT cleanup rules must show a bounded two-rule scroll area');
assert.match(styles, /\.settings-custom-cleanup-rule-list\s*\{[\s\S]*--settings-cleanup-rule-visible-count:\s*2/, 'custom TXT cleanup rules must default to showing at most two rules before scrolling');

console.log('Verified settings center data maintenance contracts.');
